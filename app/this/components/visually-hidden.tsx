import * as VisuallyHiddenPrimitive from "@radix-ui/react-visually-hidden";

export default function VisuallyHidden(props: VisuallyHiddenPrimitive.VisuallyHiddenProps) {
    return <VisuallyHiddenPrimitive.Root {...props} />;
}